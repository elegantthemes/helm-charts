apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-maintenance
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
